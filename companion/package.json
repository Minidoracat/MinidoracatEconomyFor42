{
  "name": "minidoracat-economy-companion",
  "version": "0.1.0",
  "private": true,
  "description": "Sidecar for MinidoracatEconomyFor42: tails the NDJSON event files, derives the durable watermark from global_mod_data.bin, resolves Steam accounts, and serves the intranet HTTP API for Watchcord.",
  "type": "module",
  "engines": { "node": ">=24" },
  "scripts": {
    "start": "node src/index.ts",
    "test": "node --test test/*.test.ts",
    "typecheck": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "typescript": "^5.9.3"
  }
}
