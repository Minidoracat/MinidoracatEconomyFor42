# Changelog

<!-- 撰寫規則摘要（完整版見 AGENTS.md「CHANGELOG 撰寫規則」）：
  - bullet 寫給玩家，會整段照貼 Workshop 更新說明：症狀先行、遊戲內名詞、
    禁檔名/函式名/行號/引擎術語；影響版本誠實寫清楚
  - 「> 技術要點：」（選用）只放管理員/modder 需要的行為事實；單次變更的完整
    技術原因寫在 commit message 本文（綁 diff、可 git log 搜尋、永不公開）
  - 紅線：不寫攻擊配方（安全修正只寫「強化了驗證」）、不寫玩家名/座標/Steam ID、
    不寫主機名/路徑/IP -->

所有重要的變更都會記錄在此檔案中。

格式基於 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.1.0/)，版本號遵循 `{PZ版本}-{主版本}.{次版本}.{修訂}` 格式。

## [Unreleased]

### 修正

- **交易站裝置提示不再被截斷**：右上通知改為簡短指引；完整的管理限制與設定方式保留在右鍵「交易站電台與收音說明」，可捲動閱讀與複製。更新後請完整重開客戶端。
- **交易站喇叭不再擋住角色造型**：將突出的收音機機身改成貓娘肩頂外側／機台上緣的小喇叭，四個朝向一致；貓娘正面避開胸前衣裝與蝴蝶結。既有交易站會換成新外觀，不必拆掉重建，玩家自己放置的收音機不變。伺服器與客戶端都需重新啟動。
- **重生後交易入口不再消失**：死亡後直接以新角色繼續遊戲，會恢復交易浮動圖標並保留拖曳位置；更新後請重啟客戶端。
- **浮動入口拖完就記住位置**：不必等到離開遊戲才保存；每種解析度各自記憶，切回原解析度即可還原，位置還原不會改變介面的顯示狀態。
- **收購額度不再誤報為零**：總開關暫停、幣別無法交易、上限為零與今日額度用完分開說明，保留真實設定上限及剩餘額度。
- **商店編輯不再被重複說明擠滿**：單筆與批量採固定分組，空間足夠時基本欄位並排、各幣報價對齊成表；一般規則移到完整資訊，實際變更與錯誤仍保留。批量摘要增長時不再打斷正在輸入的欄位。
- **調帳可用鍵盤完成**：修正點入金額後 Tab 無法換欄的問題，幣別、金額、反向交易編號、原因與確認操作都納入對話框內的導覽。更新後請重新啟動客戶端。
- **到期時間不再有空間仍被截短**：市場與拍賣會依實際期限預留文字寬度；窄窗市場可分行顯示日期與時間，「我的拍賣」也會計入較長的倒數。
- **信箱可辨認成交賣家**：新市場購買與拍賣得標的信件會在清單及完整資訊顯示賣家，部分領取時仍保留。舊信缺少資料就不推測補填，系統商店與退件不假造第三方賣家。更新後請重新啟動伺服器與客戶端。

- **中斷後的恢復依據更可靠**：強化伺服器端驗證與人工對帳，保留完整物品資料及來源；連續回滾、查證中途狀態變更或讀取失敗時，不再猜測補發或刪除。缺少紀錄的人工處置須明確確認資料來源，且不發放貨幣。伺服器與客戶端都需重新啟動。
- **重送交易保留原結果**：已完成交易不再因為後來停用幣別、改變價格範圍或移除商品而被回覆成未完成；確認內容不同則拒絕重用同一筆操作。拍賣保留款在調低上限或停用幣別後仍可正常退回。
- **凍結與公開設定立即反映**：凍結／解除凍結會立即更新該玩家的獎勵資格；排行榜被伺服器拒絕公開時清除舊資料，逾時則明示舊榜狀態。貨幣改名後下拉選單同步更新；未知幣別的無金流退件仍可取消取回物品。
- **大字級的商品編輯可完整操作**：鍵盤切換會把下一個欄位捲入可見範圍，商品名稱與每份件數不再被長幣名擠掉；完整錯誤留在詳情中，窄窗的分類篩選、主要操作與拍賣換頁不再互相遮擋。
- **舊物品不再一律被拒絕上架**：修正新版來源核對把可確認的舊物品擋住的問題；能對應原始信件與份數時自動補齊識別，不能確認的資料仍保留待對帳。伺服器與客戶端都需重新啟動。
- **對帳改為全服總覽**：管理台新增獨立「資產對帳」分頁，可集中查看已知待對帳與待存檔確認狀態，不必逐一搜尋玩家；離線或讀取失敗不再被當成沒有紀錄。「狀態快照」可讀完整帳號清單，窄窗與大字級也保留全文入口。伺服器與客戶端都需重新啟動。
- **對帳支援分組批次處理**：Ctrl 點列切換選取、Shift 增選目前清單的連續範圍；混合選取不再整批停用，各處置會顯示符合筆數，只執行選定操作的那一批，其餘保留選取。確認前列出選取／處理／不處理筆數、完整目標與實際件數，每筆仍重新驗證；停止、離開或失權取消未送出項目，逾時不自動重送。更新後需重新啟動客戶端。
- **詳情點開就完整顯示**：移除詳情視窗的自動收合開關，不必把滑鼠移進標題列才看到物品內容。
- **偏好與上架確認不再被下拉文字穿透**：修正上次只調整前後順序仍未解決的遮擋；市場、拍賣與管理頁的下拉文字不再浮到前景視窗上。更新後需重新啟動客戶端。
- **來源拒絕會直接說明原因**：刊登、拍賣與出售遇到來源核對問題，確認視窗會列出受影響物品、來源與下一步，不再一律要求管理員處理。能確認的舊物品可補齊識別；已證明整條來源都回滾且不再存在的待處理狀態會自動結案，不額外發物或退款。伺服器與客戶端都需重新啟動。
- **詳情記住調整後的位置**：關閉再開、換列或換來源視窗都沿用位置與大小；螢幕縮小時只移回可見範圍，不再每次重置。
- **市場與拍賣的費率說明位置一致**：說明統一放在標題下方、搜尋列上方並支援換行；「費用與規則」可讀完整內容，大字級也保留清單與換頁操作。
- **批量編輯直接反映多選**：選取兩項以上自動切換批量表單；每份件數、分類、各幣價格與收購方向等欄位有異動即可一起套用，不需另外勾選欄位。未修改欄位仍保留各商品原值，改變已有草稿的選取範圍會先確認。
- **收購說明使用右側剩餘空間**：不再把兩組表單切成三欄而空出右側，寬窗兩欄均分，窄窗與大字級仍可捲動閱讀。
- **中斷後的物品恢復更保守**：強化領取、重新託管與部分交付的來源核對；不能確認的資產會保留待對帳並提示，不會以重新登入或重啟猜測補發或刪除。
- **確認視窗不再點到後方**：購買、出售、刊登、下架與拍賣取消的確認開啟時，背景清單、賣家捷徑及通知泡不再搶走點擊。
- **領取通知只列實際結果**：全數成功只顯示已領取數量；留下或失敗的項目只有在實際發生時才列出，部分交付仍明示件數。
- **拍賣詳情不再出現未填參數**：修正識別欄位與四語欄名，長原因與紀錄內容可完整閱讀及複製。
- **大字級仍能讀完與操作**：帳號、系統、貨幣與整合詳情可捲動閱讀全文；過長的歷史篩選與結果分開顯示，種類可左右切換並保留多選。修正窄欄文字壓住操作鈕、捲動篩選中斷輸入，以及隱藏欄位仍占用鍵盤的問題。
- **查看清單不再誤觸交易**：商店、市場、我的刊登、拍賣與管理清單統一將閱讀和操作分開；購買、出售、出價、取消、領取及完整紀錄都有明確且可用鍵盤操作的按鈕。
- **操作後的紀錄會正確更新**：修正交易與管理操作完成後仍看到舊紀錄，以及連續查詢互相覆蓋的問題；查詢會等待自己的紀錄可讀取，再提供結果，失敗不會冒充空清單。
- **交付結果不再含糊**：取消後有空間會直接交還主背包，放不下才留信箱；極少數交付例外會記錄實際已交付件數，其餘保留可領，不會以死亡或重新登入猜測整封已完成。
- **Debug 模式能正常載入經濟中心**：修正前次介面更新後，Debug 客戶端點擊經濟浮動按鈕會報錯、無法開窗的問題；不必關閉 Debug 迴避錯誤，更新後請完整重開客戶端。
- **捲軸不再卡在內容中間**：修正獎勵、完整詳情與管理頁文字框的捲軸位置及高度，放大、縮小或切換檢視時會跟隨內容區。
- **選取中的紀錄更清楚**：提高滑鼠指向與選取列的次要文字對比；回滾仍有文字與刪除線，不只靠顏色辨識。
- **歷史讀取失敗可直接重試**：錢包、市場與拍賣紀錄不再誤顯示領取失敗；保留上次資料並清楚標示，清單提供重試，詳情也會保留失敗原因。
- **商品編輯不再覆蓋他人的新設定**：目錄在編輯期間更新時保留草稿並提示；套用仍會檢查原始版本，取消編輯後才能改用最新資料重新修改。目錄讀取或儲存失敗時保留原有有效設定，不會把既有目錄誤當首次啟動而覆蓋。
- **我的刊登更容易閱讀**：移除重複的賣家與數量資訊，欄位清楚標示整批價格；表頭與資料共用對齊位置，窄視窗大字級下日期可分行，完整資訊仍可閱讀與複製。
- **收購狀態更清楚**：商品已設定收購、但總開關關閉時，商店保留灰色出售按鈕並說明暫停原因；管理員可直接在商店管理頁查看及切換總開關。開關變更會立即更新所有玩家的商店，不必重新開窗。
- **拍賣紀錄讀取失敗不再冒充空白**：讀取失敗時保留原有畫面並顯示錯誤；連續修改拍賣搜尋會等待前一次讀取完成，只查最後輸入的條件。
- **日期顯示補齊年份**：錢包、信箱、市場、拍賣、管理紀錄與更新時間統一顯示 `YYYY-MM-DD`，原有時分保留；時間欄會預留完整年份的空間。
- **金流名稱更易讀**：系統發行、系統回收、管理調整、Discord 與模組帳戶，以及內建交易原因，改用對應語系名稱顯示；分錄仍保留原始代碼供查帳，玩家名稱與自行填寫的原因不變。
- **查帳內容不再擠成狹小區塊**：交易明細改為獨立閱讀視窗，長中文原因、帳戶與請求識別碼都能捲動閱讀，並可複製原始全文；小視窗會將篩選條件集中顯示，避免蓋住紀錄。
- **查詢等待與錯誤更清楚**：讀取中、逾時、失敗與上一組條件的舊結果分開標示，可直接重試或清除全部篩選；查帳內容維持實色底，提高透明視窗下的可讀性。
- **介面與內部流程整理**：金流分頁獨立管理，經濟中心的控制項、篩選、交易確認、偏好與版面也依責任分開維護；沿用共用文字、物品名稱、通知及錯誤處理，移除退役的列表元件、分頁繪製及未使用入口，並統一重複的物品查找、玩家資料處理與計數。原有查詢、交易、商店快捷、明細複製、日期與鍵盤操作維持不變。

### 新增

- **收音機可直接選市場頻道**：原生收音機、世界電台與車用電台的預設清單加入共用市場項，不占玩家原有名額，也不改掉正在收聽的頻道；選取後按調頻即可，不必再用滑桿手動對頻。頻率變更會更新清單，既有預設仍能照常新增、編輯與刪除。
- **交易站可中繼玩家語音與一般聊天**：新增預設關閉的收音開關；管理員可將終端登錄為交易站，使用同頻原生電台收發。行情摘要與收音分開控制，ATM 維持不收音；右鍵提供可捲動、複製的完整說明。仍需伺服器啟用語音，並依玩家發話鍵或語音感應設定；伺服器與客戶端都需重新啟動。
- **經濟系統專用封面**：新增末日交易工作台主視覺、動態封面與遊戲內模組海報，沿用家族一致的標題與低調掃光效果。
- **管理員可換季與安排現實天數**：新增「賽季」頁，可查看開始、截止、歷季結束日期與參與人數；具有原生角色管理權的人可調整長度或填理由手動換季。長度立即從本季原開始時間重算期限並同步在線玩家，不必先換季；0 改為手動，會立即過期的縮短拒絕。停服照計時，逾期重啟只開一個新季，不補造空季。更新後請重新啟動伺服器與客戶端。
- **生存排行榜可選季別**：排行榜新增生存類別，以每季最長單命的已記錄時間排名；死亡保留最佳成績，已結束的季別可回看，資產榜仍採即時餘額。完整期間、規則與錯誤可從詳細內容閱讀。
- **生存里程碑從新季重新計時**：跨季存活角色不再帶入前季天數立即重領；換季不清餘額或每日在線獎勵。初次導入保留已領標記並明示記錄起點，不猜測補入舊成績；死亡後不再累積生存成績。
- **賽季倒數與生存時間分開說明**：獎勵頁顯示本季剩餘現實時間與截止日，生存進度明示遊戲時間；手動、未知、到期待處理與讀取錯誤不再冒充零天。更新季期不會解除尚未回覆的領獎等待；捲動後遇到讀取錯誤仍看得到說明。
- **各分頁固定顯示玩家 ID**：經濟中心頂端常駐登入帳號，點擊或鍵盤開啟即可閱讀、複製完整名稱；長帳號不遮住餘額或原有按鈕。
- **管理限制可在面板修改**：具有原生角色管理權的人可直接調整單筆、個人每日及全服每日調帳上限；一般經濟管理員不能因此提高自己的權限。更新後請重新啟動伺服器與客戶端。
- **授權角色不必手動輸入**：可寫入、唯讀與自行調帳名單改由伺服器現有角色逐一選取，包含自訂角色；已失效的角色保留標示，讀取失敗不會假造名單。登入與撤權會同步反映。
- **自身調帳可明確授權**：預設仍停用；經授權的經濟管理角色可調整自己的餘額，沿用一般調帳的原因、限額、版本核對與稽核，仍不能凍結自己的帳戶。
- **商店、市場與拍賣支援分幣交易**：商店每項商品可設定倖存幣與貓幣的售價／收購價及方向開關；市場、拍賣建立時選幣，後續成交與退款沿用該筆幣別。混合瀏覽會標示每列幣別，按價格排序前須先選定幣別；未提供報價不再被解讀為零元。
- **商品件數、分類與限額可再次編輯**：既有商品可單筆或批量修改每份件數、商店分類與分幣價目，原生分類下拉可搭配搜尋。改件數不重算既有信件或當日已購份數；限購顯示實際作用範圍、現實重置日期及倒數。收購幣量額度按幣分開，0 明示停用，商品份數則跨幣共用。
- **每日在線獎勵可多次領取**：管理員可設定每日次數與領取間隔，顯示今日已領、尚餘次數及還需在線多久。只計實際連線時間，成功領取後才累計下一次；重新登入或換角色不重置當日次數。
- **玩家資產列表與分幣排行榜**：管理台可查所有已知玩家，包含離線與零餘額，完整排序後再分頁並可直接跳入單一帳戶。玩家排行榜依總持有排名，預設不公開他人的精確金額，管理員可調整或關閉公開榜。
- **儀表板與貨幣頁統計口徑一致**：總持有、可用、拍賣保留、持有人數及上限分開顯示，守恆差額不再與營收混淆；無全服流通上限會明示，缺漏統計或讀不到的資料不補成零。
- **所有清單共用浮動詳情**：點選紀錄開啟可移動、縮放、捲動及複製的獨立視窗，不再擠壓清單；可用關閉鈕、標題列叉號或 Esc 關閉。背景刷新不會換掉正在閱讀的另一筆紀錄，切頁、關閉來源或失去權限會收起詳情。
- **查找賣家更直接**：玩家市場／拍賣及管理刊登／拍賣共用關鍵字候選選擇器，選完整帳號後精確查詢；管理列也能查看該賣家的全部有效刊登。玩家管理頁顯示刊登／拍賣數量及上限，提供刊登、拍賣與玩家金流捷徑。
- **稽核可查歷任管理員**：管理員篩選取自所查日期區間的實際紀錄，包含已離任帳號；按帳號精確比對，切換條件不會混入前一位管理員的結果。
- **商品批改說明常駐**：清楚標示「數量」是每份件數，「限購份數」依選定範圍計算；窄欄與大字級會換行，只套用確實修改的欄位。
- **錢包與市場紀錄新增關鍵字搜尋**：可找說明、對象、物品與相關識別碼，並保留日期、類型與排序；搜尋範圍明確標示為目前已載入資料。
- **批量領取與容量預告**：每封信可單獨領取，也可一次逐封領取；空間不足會保留該封並繼續其他信件。購買前新增容量預估與寄存確認，信箱用量分別列出未領、刊登與拍賣。
- **管理介面保留商品上下文**：商品的銷售／收購紀錄會帶入目前商品；上架、停用與暫停有不同顏色及文字，貨幣設定也能直接預覽生效圖案。
- **商品全服限購與批量設定**：限購可選每人或全服，切換範圍不重置當日用量；Ctrl／Shift 多選後僅套用明確修改的欄位，整批一起驗證與儲存。
- **查帳條件與全文更完整**：玩家及物品可精確選取，關鍵字支援包含或完全相同，並可由玩家收據直接查看該筆分錄。稽核摘要能按需讀取全文，缺檔、失敗與歧義均有明確狀態。
- **物品名稱支援多語搜尋**：使用目前語系、真實英文與物品代碼查詢；名稱來源不完整時列出受影響的 MOD 並保留可用搜尋，不把缺漏當成確定沒有交易。
- **玩家與管理台共用圖示導覽**：標題列可切換圖示加文字或窄圖示列，兩個視窗分別記住選擇；收合時不重新載入資料、不丟失草稿，圖示保留完整提示。
- **玩家工作區重新整理**：錢包對帳單、商店、市場、拍賣與信箱使用完整工作區；搜尋與分類移到清單上方，餘額詳情獨立閱讀，所有存活獎勵都能捲動查看。
- **玩家所有頁面支援鍵盤**：選列、購買、出售、出價、領取、完整資訊及偏好設定都能用鍵盤操作；交易摘要可捲動，確認與取消按鈕不再被大字級推到小視窗之外。
- **預設視窗更大、偏好入口更清楚**：玩家視窗採用較大的預設尺寸，原有自訂大小仍保留；透明度與通知秒數集中到側欄「設定」，儲存失敗會明確提示。
- **管理改為獨立大視窗**：經濟中心的「管理」入口會開啟獨立管理台，以側欄切換功能，保留自己的視窗位置與大小，不再占用玩家錢包的內容區。兩個視窗的輸入焦點與彈出層各自管理；更新後須重新啟動伺服器與客戶端。
- **商店改為清單加編輯區**：商品列不再塞滿加減鈕與開關；選取後可集中修改售出、收購與每日限制，一次套用。新增商品可搜尋目前載入的物品，包含其他模組的物品；收購總開關以明確的狀態文字與獨立操作顯示。
- **白名單分開管理分類與物品**：分類規則使用勾選狀態，指定物品提供「允許／排除／依分類」三種選擇及生效來源；新增物品規則使用搜尋選物器，已失去物品資料的舊規則也可移除。
- **新管理流程與市場支援鍵盤**：商品編輯、白名單、完整資訊、交易確認與背包選物可用鍵盤操作；選列有可見標示，關閉彈出層會回復焦點，不會操作到後方視窗。
- **拍賣紀錄**：拍賣頁新增「紀錄」，可查看出價者、出價時間、每口金額及成交結果；每場拍賣旁也能直接開啟該場紀錄。管理員可在「拍賣 → 全部紀錄」集中查詢全服拍賣，依拍賣 ID、帳號或物品搜尋，並使用日期、種類與分頁篩選；回滾紀錄會標示，查詢涵蓋最近兩個月。
- **管理員金流紀錄**：新增「金流」頁，集中查看商店銷售／收購、市場、拍賣、獎勵、管理調帳、模組整合與 Discord 存入。商店管理頁可直接開啟「銷售紀錄」或「收購紀錄」；支援來源、幣別、日期及關鍵字搜尋，點選交易可查看各帳戶分錄、異動前後餘額與原因，回滾交易另有標示。
- **日期篩選支援日曆選取**：所有日期欄位保留手動輸入，旁邊新增日曆按鈕，可切換月份／年份、選今天或清除日期；支援方向鍵、Enter 選取及 Esc 關閉。輸入完成後會整理成 `YYYY-MM-DD`，不存在的日期不再自動進位成下一個月。
- **金流新增帳戶類別篩選**：可按玩家、系統發行／回收、管理調整、模組、Discord 或其他系統帳戶查詢，並與來源、幣別、日期及關鍵字一起使用；符合任一相關帳戶便顯示完整交易。
- **管理金流支援鍵盤操作**：可用 Tab、方向鍵、Enter 與 Escape 操作查詢、明細與日曆；焦點有明確框線，截短按鈕保留完整提示。
- **經濟中心新增「管理」入口**：管理員可在獨立管理台查詢玩家帳戶、調整餘額、凍結或解除凍結帳號、修改貨幣顯示名稱／啟用狀態／存入比率，並檢視稽核紀錄與伺服器狀態；唯讀管理角色只能查詢，一般玩家看不到這個入口
- **餘額上限可設定**：沙盒新增「餘額上限（保險絲）」作為所有貨幣的預設，管理面板的貨幣設定可對單一貨幣另設覆寫或清除；系統頁的資料路徑改只顯示資料夾之後的部分（複製時仍是完整路徑）
- **調整餘額前會先顯示前後餘額與今日剩餘額度**：送出後若餘額已被其他交易改動，會要求重新確認，避免用過期資料調帳
- **貨幣資訊更易辨識**：倖存幣與貓幣使用彩色圖示；視窗右上圖示再次加大，保留款與餘額採相同字級，滑鼠移入可查看完整貨幣名稱。
- **其他模組可以接上經濟系統**：提供給模組作者的伺服器端介面（扣款、發放、查餘額），每個模組有獨立的每日發放／回收額度，管理頁新增「整合」分頁可調整額度與停用；模組交易在對帳單會顯示來源模組與原因
- **管理頁玩家搜尋有候選清單**：輸入幾個字就列出符合的帳號（線上的排前面），點一下直接查詢；調整餘額視窗放大、原因只要有填即可（不再限 10–200 字）
- **伺服器意外關閉後的對帳單標記更可靠**：未存檔就關閉的那段時間內發生的交易，重啟後在「本月／上月」對帳單會正確標為「已回滾」，不再看起來像錢憑空消失
- **被回滾的紀錄到處都看得到**：錢包「最近」改讀收據檔（最近兩個月、最新 20 筆），被回滾的交易會畫線標示；管理頁的玩家收據與稽核（含「全部」）也會併入被回滾的筆數，另有「已回滾」篩選只看這些
- **帳號被凍結時玩家會知道**：經濟中心頂端出現紅色提示、簽到按鈕停用，凍結／解除當下會跳通知
- **管理頁新增「設定」分頁，可直接調整沙盒設定**：分組列出（獎勵、管理限制、貨幣與存入、一般），每項附說明與單位，可搜尋；開關、加減步進與編輯視窗即時生效、不需重啟伺服器，獎勵相關的改動會立刻推到每個線上玩家的獎勵頁；已覆寫的項目會標示預設值並可單項或整組重設回伺服器設定檔；管理員調帳額度與角色名單須有原生角色管理權才能修改。
- **視窗預設大小跟著螢幕走**：玩家視窗約占螢幕寬度 84%、高度 86%，換解析度時自動收回畫面內；拖過大小後標題列會出現「重設大小」，一鍵回到預設並置中；原有自訂大小不會在更新後被覆蓋。
- **調帳／凍結原因不再限制字數**：有填就好（沙盒「調帳原因最少字數」選項移除）；調整餘額視窗的原因欄位放大
- **伺服器可自訂貨幣圖示**：管理員把 PNG（64 KB 以內）放進伺服器資料夾後在管理頁「貨幣設定」按「重讀圖示」，所有玩家會自動下載並套用；刪掉檔案再按一次就回到內建圖示
- **經濟終端**：管理員可在建造選單建造「經濟終端」（ATM 造型）或「貓娘經濟終端」（貓娘服務機器人造型；兩者都是自有四面向貼圖，建造時按 R 轉向），或直接把地圖裡現成的終端機櫃登錄，再「登錄為經濟終端」；購買、上架與領取物品必須站在終端旁（2 格內），遠端只能看，沙盒「允許遠端開啟唯讀分頁」關閉時視窗只能在終端旁開啟。任何人右鍵終端都有「使用經濟終端」直接開商店。終端搬不走、殭屍打不壞、只有管理員能用大鎚或右鍵「拆除經濟終端」移除
- **系統商店**：以倖存幣向伺服器購買固定價物品，每人每日限購依商品而定；買到的物品直接進背包，放不下的留在信箱。商品目錄由伺服器資料夾的 `catalog.json` 決定（首次啟動會自動產生範例），管理頁新增「商店」分頁可即時上下架、改價、改限購，改動直接寫回 `catalog.json` 並推送給所有在線玩家；商品旁同時顯示原文名稱，搜尋可用中文、原文或物品 ID
- **信箱**：經濟中心新增「信箱」分頁，顯示待領取的物品與數量，站在終端旁按「領取」即可；角色死亡後未領取的物品仍會保留。對帳單的商店購買列會寫出買了什麼、幾件
- **玩家市場**：經濟中心新增「市場」分頁，在終端旁可把背包裡的物品上架（固定價、刊登費）、瀏覽／搜尋／分類／排序所有玩家的刊登並購買（成交稅由賣家負擔，物品進背包或信箱）、取消自己的刊登（退回信箱）；到期未售出自動退回。上架選物是放大的背包網格，預設只顯示可上架的物品，游標移到物品上會顯示原文名稱、耐久／用量與不能上架的原因。可上架的物品由伺服器資料夾的 `whitelist.json` 決定（預設：工具、武器、材料、彈藥、醫療、不腐敗食物、未讀書籍、液體容器等；容器、衣物、會腐敗食物、讀過的書、裝備中或損壞的物品不能上架），含未知模組資料的物品會被拒絕以免重建時遺失。武器上架時配件會自動拆回背包
- **市場沙盒選項**：刊登費％、成交稅％、刊登天數、每人刊登上限、定價上下限、每人信箱容量，管理頁「設定」可即時調整
- **信箱容量**：每人信箱格數由沙盒「每人信箱容量」決定（預設 50）；待領取的物品與掛在市場上的刊登各佔一格，信箱頁顯示「信箱 N／上限」；滿了不能購買或上架（上架選物器會說明），取消、到期與管理員下架退回信箱不受限、不會超過上限
- **市場電台**：交易站（不含 ATM）會定時用收音機廣播市場摘要——目前刊登數、賣家數與最新上架的三件（含數量與價格）；把收音機調到「市場電台」頻道（預設 101.1 MHz）、在交易站收訊範圍內（預設 500 格，可設全地圖）就聽得到。沙盒可調廣播間隔（預設 10 分鐘，0 為關閉）、頻率、範圍與語言
- **拍賣場**：經濟中心新增「拍賣」分頁。在終端旁可把可上架的物品送去拍賣（起標價＋時長 6–72 小時，刊登費以起標價計）；瀏覽所有拍賣、出價（出價的幣會先保留，被超過就立刻退回；自己加價只多扣差額；下一口至少要比目前高 5％）；結標時最高出價者得標，物品進得標者的背包或信箱，賣家收到成交價扣稅後的金額；無人出價退回賣家信箱；沒人出價前賣家可以取消。「我的拍賣」看自己賣的與出價中的（領先／被超過）。伺服器重啟時依停機長度處理：5 分鐘內照常、5 分鐘到 24 小時把所有拍賣延長同樣時間、超過 24 小時全部取消退回並退還出價。市場電台會預告即將結標的場數。沙盒：拍賣最短／最長時長、每人同時拍賣數、最低加價幅度
- **管理頁新增「拍賣」分頁**：檢視所有進行中的拍賣、取消（退回最高出價、物品退回賣家信箱、寫入稽核）；伺服器狀態顯示進行中拍賣數
- **系統收購（預設關閉）**：伺服器可以向玩家收購目錄中標示「收購」的物品換取新發行的幣。管理員在目錄頁替單一商品設定收購價（必須低於售價）、開關與每日收購份數；沙盒「開放系統收購」是總開關（關閉立即停止），另有每人每日與全服每日的發行上限（以發行量計，銷毀不會回補，超過整筆拒絕）。玩家在商店列看到「出售 價格」就能賣：只收全新狀態（滿耐久、未使用、未改名），對話框顯示背包裡可賣幾件、份數、收款與今日剩餘額度。儀表板新增「發行合計／其中收購／銷毀合計」三欄（今日／7 天／30 天），伺服器狀態列出收購開關與今日發行量，建議先觀察再開放
- **Discord 積分存入（伴隨程式）**：伴隨程式（companion）提供 Watchcord 用的內網 API：查目前貨幣與兌換比率、依 Steam 帳號列出遊戲帳號、建立存入訂單、查訂單狀態。訂單寫進伺服器資料夾的 `inbox/`，遊戲伺服器每 2 秒讀取並直接把貓幣存進玩家錢包（來源科目 `EXTERNAL_DISCORD_貓幣`，可與 Discord 側對帳），玩家在線會收到通知，錢包對帳單看得到「Discord 存入」；伺服器在存檔前崩潰也會自動補入一次、不重複。比率改動後，Watchcord 已受理的舊比率訂單仍照當時比率入帳；每人每日與全服每日存入上限沿用貨幣設定（管理頁「貨幣」可調）。v1 只進不出，退款走管理員補償

> 技術要點：companion 新增 `GET /currencies`、`POST /orders`、`GET /orders/{id}`；訂單檔由 companion 單一寫入、事件 `exchange.deposited`／`exchange.failed` durable 後才刪除；重送同一訂單回原結果。
- **管理頁新增「刊登」分頁**：檢視所有刊登、強制下架（退回賣家信箱並稽核）；「玩家紀錄」可輸入帳號查看該玩家最近兩個月的市場紀錄
- **同狀態的物品可以合併成一筆刊登**：上架選物器把一樣的東西（例如一疊木板）合成一格並標示數量，定價時輸入要賣幾件、價格是整批；瀏覽頁顯示 ×N，買家一次買下整批
- **賣家即時通知與信箱泡泡**：刊登售出、到期退回或被管理員下架時，賣家在線上會立刻收到通知；右側浮動按鈕與「信箱」分頁以紅色泡泡顯示待領取件數
- **市場「紀錄」頁**：最近兩個月自己的上架、售出、購買、取消、到期、下架與崩潰後自動重建；伺服器未存檔就關閉而被回滾的會標示「已回滾」
- **稽核頁看得懂了**：動作、對象與變更都有中文（例如「上架白名單 · 螺絲起子 · 物品：依分類 → 禁止」），點一筆展開完整原因，可一鍵複製物品名稱或 ID
- 市場瀏覽的分類改用遊戲內譯名（不再顯示 MaterialWeapon 這類原始分類名）；快速切換排序不再偶爾失效；管理員改白名單時，開著的上架選物器會立即更新
- **市場表頭可以點擊排序**：物品、數量、賣家、價格、到期任一欄都能點，再點一次反向；上方排序選單提供相同選項。
- **紀錄、對帳單與稽核都能篩選**：依類型多選、日期從／到、時間或金額排序，並每 25 筆分頁
- **白名單狀態清楚標示**：分類以勾選狀態表示允許與否，物品則顯示「允許／排除／依分類」及實際生效來源，個別指定過的規則可明確改回依分類。
- **視窗不透明度可調**：側欄「設定」（或「模組選項」）可調整經濟中心底色的不透明度與通知停留秒數（2–10 秒），兩者都會記住。
- 通知可顯示三行，管理員下架的理由不再被截斷；待領取泡泡改為圓形紅底；按鈕送出後會先停用直到伺服器回覆，連點不再造成「指令逾時」

> 技術要點：本次導覽要求 MinidoracatUIFor42 API rev 6，Economy 與 UIFor42 需一起更新；rev 6 包含多行通知與 16 個新增導覽圖示，舊框架不會啟用新版經濟介面。
- **管理頁新增「白名單」分頁**：可直接調整可上架的分類，對單一物品指定允許、排除或依分類，改動立即寫回伺服器清單並生效；分類及已設定規則各自搜尋，新增物品規則可搜尋目前載入的物品。容器、衣物、鑰匙、家具、地圖、動物仍不開放上架，不受個別名單設定影響。
- **更多東西能上架**：會腐敗的食物可以上架，託管期間照常腐敗（領出來就是當下應有的新鮮度，過期就過期）；讀過的書可以上架；無線電／對講機可以上架，頻道、電量、開關、音量會跟著物品走；被改過名字的物品領出來仍是原名；物品身上的模組資料會完整跟著走（先前一般木板會因為遊戲本身寫在物品上的資料而被拒絕上架）

> 技術要點：管理指令由伺服器重新驗證角色與錢包版本；同一請求重送不會重複入帳，稽核環只保留原因前 40 字，完整原因寫入稽核檔。伺服器端 companion（Watchcord 對接程式）改以 TypeScript 撰寫，Node 24 直接執行、執行期仍無第三方依賴，API 與資料格式不變。自訂圖示存在伺服器資料夾與玩家本機快取，不進存檔；玩家端每張圖最多下載三次、每場最多 16 張，快取以伺服器位址區隔。商店購買的扣款與信箱項在同一份世界存檔內一起生效，物品進背包是緊接著的「領取」；伺服器崩潰後登入會自動比對信箱與背包（補送、標記或收回），不會重複給物品，也不會讓已退款的購買留下物品。

## [42.20.4-0.1.0] - 2026-08-31

### 新增

- **初始專案骨架**：建立 Build 42 MOD 目錄、開發工具與發版驗證流程，尚未加入遊戲內經濟功能
